### LocalDate, LocalTime, Instant, Duration, Period 클래스

1. LocalDate와 LocalTime 사용

```java
class LocalDateNLocalTime {
    public static void main(String[] args) {
        LocalDate date = LocalDate.of(2011, 8, 21);
        int year = date.getYear();
        Month month = date.getMonth();
        int dayOfMonth = date.getDayOfMonth();
        DayOfWeek dayOfWeek = date.getDayOfWeek();
        int len = date.lengthOfMonth();
        boolean leapYear = date.isLeapYear();

        LocalTime time = LocalTime.of(13, 23, 30);
        int hour = time.getHour();
        int minute = time.getMinute();
        int second = time.getSecond();
    }
}
```

2. 날짜 시간와 시간 조합

```java
class LocalDateTime {
    public static void main(String[] args) {
        LocalDateTime dt1 = LocalDateTime.of(2017, Month.APRIL, 21, 13, 35, 20);
        LocalDateTime dt2 = LocalDateTime.of(date, time);
        LocalDateTime dt3 = date.atTime(13, 45, 20);
        LocalDateTime dt4 = date.atTime(time);
        LocalDateTime dt5 = time.atDate(date);
    }
}
```

3. Instant 클래스 : 기계의 날짜와 시간
4. Duration과 Period 정의

```java
class Duration {
    public static void main(String[] args) {
        Duration d1 = Duration.between(time1, time2);
        Duration d2 = Duration.between(datetime1, datetime2);
        Duration d3 = Duration.between(instant1, instant2);

        Period tenDays = Period.between(LocalDate.of(2016, 9, 20),
                LocalDate.of(2016, 9, 30));
    }
}
```

### 날짜 조정, 파싱, 포매팅

```java
class LocalDateTest {
    public static void main(String[] args) {
        // 절대적인 방식
        LocalDate date1 = LocalDate.of(2016, 9, 20);
        LocalDate date2 = date1.withYear(2011);
        LocalDate date3 = date2.withDayOfMonth(25);
        LocalDate date4 = date3.with(ChronoField.MONTH_OF_YEAR, 2);

        // 상대적인 방식
        LocalDate date1 = LocalDate.of(2016, 9, 20);
        LocalDate date2 = date1.plusWeek(1);
        LocalDate date3 = date2.minusYears(6);
        LocalDate date4 = date3.plus(6, ChronoUnit.MONTHS);
    }
}
```

### 다양한 시간대와 캘린더 활용 방법
1. 시간대 사용하기

```java
import java.time.ZoneId;
import java.util.TimeZone;

class ZoneIdTest {
    public static void main(String[] args) {
        ZoneId romeZone = ZoneId.of("Europe/Rome");
        ZoneId zoneId = TimeZone.getDefault().toZoneId();
    }
}
```

2. UTC/Greenwich 기준의 고정 오프셋
3. 대안 캘린더 시스템 사용하기