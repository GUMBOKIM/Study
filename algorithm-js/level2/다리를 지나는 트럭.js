function solution(bridge_length, weight, truck_weights) {
    truck_weights.reverse();
    let answer = 0;

    let bridge = new Array(bridge_length).fill(0);

    let checkBridge = () => {
        bridge.reduce()
    }


    return answer;
}

console.log(2, 10, [7, 4, 5, 6])