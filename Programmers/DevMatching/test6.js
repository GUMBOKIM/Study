function solution(k, dic, chat) {
    let chatWords = [];

    const convertWord = (originWord) => {
        const word = originWord;

    }

    const checkWord = (originWord, slangWord) => {
        let result = false;
        let commaNumber = originWord.

        // 사이즈 체크


        return result;
    }

    const execute = (chatWord) => {

    }

    chat.forEach(chatWord => execute(chatWord));

    return answer;
}

const countCharacterInString = (string, char) => {
    let copy = string;
    let originLength = copy.length;
    copy.replaceAll(char, "");
    let afterLength = copy.length;
    return originLength - afterLength;
}